# Privacy

qwen-audio-agent 的编排运行时由用户自行部署，不包含内置遥测、广告分析或自动崩溃上报。
语音对话、工具调用与后台工作可能与用户选择的外部服务交换数据；是否离开本机取决于配置。

## 数据流向

- 默认情况下，麦克风音频、实时转写上下文和模型回复请求会发送到阿里云
  DashScope 的 Qwen Audio Realtime 服务。若用户配置其他兼容服务，则数据发送到
  该服务。
- WebUI 仅在用户主动打开相机并开启实时视觉后，才会将有界 JPEG 帧经 Gateway
  发送给当前 Realtime Provider。停止实时视觉即停止发送；Gateway 不把这些帧写入
  对话历史、任务状态或本地文件，Provider 的后续处理受其隐私政策约束。
- 工作指令、所需附件与上下文会发送给用户选择的后台 Agent，结果再返回前台。
  后台可通过 ACP、A2A 或自定义接口接入本机或远程服务，并使用其配置的模型、
  工具、MCP 与项目文件；这些服务各自的隐私政策同样适用。
- 开启前台 MCP 或联网工具后，调用参数会交给对应工具或服务，返回内容可进入模型上下文。
  `stdio` MCP 在 Gateway 主机启动进程；HTTP/SSE MCP 可连接远程服务。搜索服务会收到
  搜索词，网页读取会访问目标站点。是否调用后台 Agent 与这些前台工具的数据流无关。
- 记忆与资料库默认实现保存于 Gateway 主机。启用文本模型辅助整理时，相关对话或文档
  片段会交给所配置的模型；复杂文档转换使用后台的隔离执行会话，检索片段会交给语音前台。
  若配置外部 Memory/Knowledge Provider，查询、写入与文档处理使用该 Provider；
  声明支持会话或音频观察的记忆 Provider 还可能接收对话或音频。请检查其配置与隐私政策。
- Markdown 中的远程图片、音频和视频默认不会自动加载。用户点击加载或打开链接
  后，目标站点可能获知用户的 IP 地址和浏览器信息。

## 本地数据

Gateway 的用户档案、长期记忆、任务状态、后台工作目录和配置默认保存在
`~/.config/qwaudio/`。API Key 等凭据不应写入仓库、对话或用户档案。卸载应用不会
自动删除该目录，避免意外丢失用户数据。桌面版的皮肤、界面偏好与连接凭据保存在
Electron 的客户端数据目录，移动端与浏览器也各自保存连接状态；远程连接不会把
Gateway 的整份配置同步到客户端。

运行日志默认保存在 `~/.config/qwaudio/logs/`，不会自动上传。日志系统会脱敏常见
凭据字段和 Bearer/API Key 文本，并且默认不记录音频、转写正文、模型回复正文、
任务目标或任务结果。诊断日志仍可能包含本机路径、所选 Agent/模型、Session/Task
标识和错误信息；分享日志前请再次检查，卸载应用也不会自动删除这些日志。

## 远程部署

Gateway 默认只监听本机。显式 `--lan` 允许可信局域网访问，但 HTTP/WS 不加密传输；
不要将这一入口转发到公网。跨网络可使用官方 Tailscale 的私有 Tailnet 与 HTTPS Serve，
或自行部署带可信证书的 HTTPS 反向代理。客户端需配对或使用访问 Token；同一 Tailnet
不替代 Gateway 的设备授权。

部署者负责防火墙、代理、日志保护与数据保留策略。配对链接含设备凭据，不应公开分享；
设备丢失或链接泄露后应撤销凭据。配置 Tailscale 时，其网络连接与账户信息处理也受
Tailscale 隐私政策约束。操作说明见[远程连接与配对](docs/operations/remote-access.zh.md)。

## 项目边界

本项目无法控制用户选择的模型、后台 Agent、MCP、记忆、知识库、网络服务及第三方
站点如何处理数据。正式使用前，请审阅相关服务的隐私条款并选择合适的部署方式。
