# Copy to .env here, or export variables in your shell.
# The provider key stays on the Gateway. Never enter it in the browser UI.
DASHSCOPE_API_KEY=

# Optional for remote deployment; localhost does not need a TURN server.
# QWAUDIO_WEBRTC_ICE_SERVERS=[{"urls":"turn:turn.example.com:3478","username":"temporary-user","credential":"temporary-password"}]
# QWAUDIO_WEBRTC_ICE_TRANSPORT_POLICY=all
